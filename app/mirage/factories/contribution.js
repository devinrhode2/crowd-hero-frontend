import Mirage from 'ember-cli-mirage';

export default Mirage.Factory.extend(
  {type: 'MyString', contributor: 'MyString', causes: [], event: 'MyString', donationAmount: 'MyString', hours: 42, skilled: false }
);
